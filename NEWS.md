# convmoment 0.2.3

* Version bump for CRAN submission (0.2.2.9000 -> 0.2.3)
* Fixed BugReports URL to point to GitHub repository
* Added `vctrs` to Suggests for test compatibility
* Vignette now uses only exported functions
* Comprehensive test suite (11 tests) covering raw-to-raw, raw-to-central, central-to-raw transformations, round-trip validation, and edge cases

# convmoment (development version)

* Added `na_rm` param

# convmoment 0.2.1.9000

* Added the function `get_moments()`, which can calculate moments up to `rth`
  degree.
* Added package-level documentation.

# convmoment 0.0.1.9000

* Added the working function `conv_moment_all()`